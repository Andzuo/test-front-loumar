import { createTheme } from "@mui/material";

export const theme = createTheme({
   palette: {
      primary: {
         main: "#FFB800",
      },
      secondary: {
         main: "#475467",
      },
      whatsappButton: {
         main: "#25D366",
      },
   },
});
