import { createTheme } from "@mui/material";

export const theme = createTheme({
   palette: {
      primary: {
         main: "#FFB800",
      },
      secondary: {
         main: "#475467",
      },
      whatsappButton1: {
         main: "#25D366",
      },
      whatsappButton2: {
         main: "#fff",
      },
   },
});
